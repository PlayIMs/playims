# PlayIMs environment variable map
#
# Use `.env` for local Node/tooling (for example Drizzle config and scripts).
# Use `.dev.vars` for Wrangler/Cloudflare local runtime (`platform.env`).
# Auth vars are REQUIRED in runtime env (`.dev.vars` locally, Cloudflare env in prod).
# Auth vars in `.env` are optional, only for non-Wrangler Node/test workflows.

# -----------------------------
# .env only (Node/tooling)
# -----------------------------
CLOUDFLARE_ACCOUNT_ID=cloudflare_account_id
CLOUDFLARE_DATABASE_ID=d1_database_id
CLOUDFLARE_API_TOKEN=cloudflare_api_token

# -----------------------------
# .dev.vars required (auth runtime)
# .env optional mirror (only if needed for Node/test tooling)
# -----------------------------
AUTH_SESSION_SECRET=replace_with_long_random_secret
AUTH_PASSWORD_PEPPER=replace_with_distinct_long_random_secret
AUTH_PASSWORD_PBKDF2_ITERATIONS=__SET_A_HIGH_INTEGER_VALUE__
AUTH_SIGNUP_INVITE_KEY=replace_with_invite_key
