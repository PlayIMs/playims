interface:
  display_name: 'PlayIMs Data Table Builder'
  short_description: 'Build PlayIMs tables that match the offerings page'
  default_prompt: 'Use $playims-data-table-builder to build this table so it matches the PlayIMs offerings-page table styles and component pattern.'
